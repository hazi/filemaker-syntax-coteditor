While(
  [ ~count =   1;
    ~total =   0;
    $a[1]  =   0;
    $a[2]  = +20;
    $a[3]  = -.1 ];
  not IsEmpty($a[~count]);
  [ ~total = ~total + $a[~count];
    ~count = ~count + 1 ];
  "Total = \"" & ~total & "\";¶Count = \"" & ~count - 1 & "\""
)